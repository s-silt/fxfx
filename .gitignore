__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
*.apk
out/
.apkscan_cache/

# PyInstaller 构建产物(大，绝不入库)
/build/
/dist/
# GUI 发行 zip(build_exe.py 打出的 onedir 压缩包，大，绝不入库)
/fxapk-gui-*.zip

# adb 下载缓存(build_exe.py 下 Google platform-tools 解压取 adb.exe + 2 dll)
/.platform_tools/

# 入口自检临时 venv(用 mangled 路径建的一次性 venv，绝不入库)
*.tmp_epcheck/

# 烟测/分析输出目录(任何 out_* 变体，绝不入库；注意 out/ 已在上方单列)
out_exe/
out_exe*/
out_chk/
out_review/
/out_*/

# 样本数据与工作产物(不入库)
ybku/
/run_real.py
/run_both.py
/.wf_keyvalue_plan.md
/.wf_*.md
/_perf_probe*.py
*.pdf
